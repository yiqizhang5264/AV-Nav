# 实验实施状态

已在 `D:\zyq\AV_nav` 完成本地项目，服务器目录为 `/home/zyq/AV-Nav`。本地通过 HTTPS 向 GitHub 推送，服务器通过已有 GitHub SSH 身份推送；已实际验证本地推送、服务器拉取、服务器提交结果推送和本地拉取。服务器密码未写入任何文件。

原 Word 研究方案已改为可执行实验协议，原稿保留在本地 `backups/AV-Nav_研究方案_原稿_20260907.docx`。配套 [README](../README.md) 包含运行命令。

## 已完成

- 固定官方 VLFM 提交，复用服务器既有 Python 环境、模型与 HM3D 资源，不改动原有 `/home/zyq/vlfm` 工作区。
- 实现候选观测、共同验证器、自由空间路径过滤、尺度自适应视点、随机与最近视点对照、预算、扫描、融合和回退。
- 完成 13 项核心与流程测试：含深度单位、不可达路径、禁止穿墙、种子复现、互斥场景划分和逐回合配对校验。
- 完成 baseline 与默认 active 各 5 个训练回合、100 步上限的冒烟测试。两组 SR 均为 0.2，SPL 均约 0.1595，默认 active 的主动触发数为 0。该结果只说明运行链路一致，不支持主动视点有效性结论。
- 完成单回合强制触发诊断：实际发生可达视点选择、移动、新视角采样、融合与未决回退。该测试采用单独的诊断阈值和预算，不与正式方法结果混用。

## 接下来的研究工作

1. 用互斥训练场景建立 Calibration-300 与 Dev-100，核查场景资源与原始 episode ID 映射。
2. 审计保存的候选裁剪与分数，确定默认触发覆盖率低的原因并校准阈值。不能根据最终验证集选参数。
3. 先在开发集比较 single、passive、random、nearest、fixed_radius 与 active；固定模型、候选入口和验证预算。
4. 对局部点云实例关联、遮挡下距离估计、二维可见性、重复观测与动作成本做针对性审计。
5. 冻结参数后做完整任务预算、多 seeds 的独立验证。VLFM 上的收益成立后才进入 DSVR-Nav 集成。

尚未完成正式基线复现、阈值校准、大规模消融或 DSVR-Nav 集成。任何小样本、强制触发或局部质量改善都不能替代这些实验。

首轮原始实验结果与运行配置见 [smoke_20260907](../results/smoke_20260907/README.md)；工程诊断见 [diagnostic_20260907](../results/diagnostic_20260907/README.md)。
